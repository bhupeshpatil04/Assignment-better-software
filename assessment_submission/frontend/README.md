
# Frontend (React)

This is a minimal React app that expects the backend to be accessible under the same origin.
To run:
1. npm install
2. npm start

For development, you can proxy API calls to Flask server or run both on same host & port via build.
